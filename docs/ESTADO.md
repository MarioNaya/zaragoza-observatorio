# Estado del proyecto y arranque de sesión

Última actualización: 2026-09-08, octava sesión (**fase 2: `citizen` implementado y desplegado**). La fase 1 está cerrada, la instancia está en marcha en <https://observatorio-production-ed20.up.railway.app> y su volumen está respaldado (ADR-010); lo que le queda es tiempo (§4). En la fase 2 están hechos los tres pasos previstos: **S2.1** (la API no resuelve la junta), el módulo **`geo`** y ahora **`citizen`**, cuya decisión de diseño principal fue **qué no pedir**: S2.2 comprobó que el parámetro `fl` recorta de verdad la respuesta, así que el texto libre de las quejas —que el ayuntamiento publica sin anonimizar— no se descarga, no llega a `raw_payload` y no tiene columna donde guardarse (**ADR-012**). Con él llega también la tabla de sinónimos de nombres de junta que ADR-011 §5 dejó pendiente, con sus cuatro entradas observadas. **Está en producción con las 89.432 quejas cargadas**, y la medición de memoria que quedaba pendiente está hecha: la hipótesis del metaspace era falsa y no hay ninguna bandera que ajustar (§5, ADR-009). La lección de la sesión no está en el código sino en el método: ejecutar la ingesta de verdad destapó dos fallos que ningún test veía, y **una ingesta se comprueba contando identificadores distintos, no filas**. Este documento es el punto de entrada de cada sesión de trabajo: qué está hecho, qué decisiones rigen, cómo se arranca el entorno y cuál es el siguiente paso concreto. Se actualiza al cerrar cada sesión.

## 1. Dónde estamos

| Fase (SPEC.md §3) | Estado | Evidencia |
|---|---|---|
| Paso 2 de §10: esqueleto | **Hecho** | Spring Boot 4.1.1 + Modulith 2.1.1 + Java 21, PostGIS en Compose y Testcontainers, Flyway, tests de arquitectura |
| Fase 0: spikes S0.1–S0.6 | **Hecho** | Seis clases `@Tag("spike")`, fixtures reales, informes en `docs/spikes/` |
| Paso 4 de §10: revisar la especificación | **Hecho** | `SPEC.md` v0.14 (revisada al cerrar cada sesión); ADR-001..012 |
| Fase 1: `ingestion` + `catalog` (eje declarado) | **Hecho** (2026-09-06, segunda sesión) | Integrado en `main`; API REST, contrato OpenAPI, instantáneas diarias |
| Higiene del repositorio | **Hecho** (2026-09-06) | Fixtures redactados, historial limpiado con `git filter-repo`; regla 22 |
| Fase 1: eje observado de la frescura (S1.1) | **Hecho** (2026-09-06, tercera sesión) | `DistributionHttpObserver`, `ObserveDatasets`, Flyway V005, API con filtro/orden/resumen por método; ADR-005 |
| Fase 1: **cruce con el Swagger** (S1.2) | **Hecho** (2026-09-06, cuarta sesión) | Rama `feat/swagger-federated`: spike `S12ApiInventorySpike` + informe; `ResponseShape.DOCUMENT` en `ingestion`; `ApiEndpoint`, `SwaggerJsonTranslator`, `ApiInventoryIngestionJob`, Flyway V006; `apiEndpoints` en el detalle, `GET /catalog/api-tags`, `GET /catalog/api-endpoints`, `apiInventory` en `summary`; la observación prueba `<declarado>/list` (ADR-006). Comprobado contra la API real (§5) |
| Fase 1: **`federated`** (S1.3) | **Hecho** (2026-09-06, cuarta sesión) | Spike `S13FederationSpike` + informe; `Pagination.pages` y `ResponseShape.RESULT_ITEMS` en `ingestion`; `FederatedDataset`, `FederationJsonTranslator`, `FederationIngestionJob`, Flyway V007, baja de lo no visto por evento; `federated`/`federatedUrl`, filtro `federated`, `GET /catalog/federation`, `federation` en `summary` (ADR-007). `.\mvnw.cmd verify` en verde (122 tests); comprobado contra datos.gob.es real (§5) |
| Fase 1: **preparación del despliegue** | **Hecho** (2026-09-06, quinta sesión) | `Dockerfile` multietapa, `.dockerignore`, perfil `prod` completo, `compose.prod.yaml`; ADR-008 y `docs/despliegue.md`. Imagen construida y arrancada en local contra PostGIS: 436 fichas, 497 operaciones, 369 federados desde la imagen de producción (§5) |
| Fase 1: **instancia desplegada** | **Hecho** (2026-09-07, quinta sesión) | Railway, proyecto `zaragoza-observatorio`, región `europe-west4`: servicio `postgis` (imagen `postgis/postgis:17-3.5` con volumen) y servicio `observatorio` desde `main`. Verificado en la URL pública: 436 fichas, 497 operaciones, 369 federados (§5) |
| Fase 1: **memoria y coste acotados** | **Hecho** (2026-09-07, quinta sesión) | ADR-009: topes de JVM explícitos en el `Dockerfile` y `tomcat.threads.max`. Medido en local (367,8 MB estables) y en la instancia: **688 → 356 MB**, ~5,7 $/mes en total (§5) |
| Fase 1: **copias del volumen** | **Hecho** (2026-09-08, sexta sesión) | ADR-010: `DAILY` (6 días), `WEEKLY` (27) y `MONTHLY` (89) sobre `postgis-volume`, activadas por la API pública porque **la IaC no las aplica**; copia manual `verificacion-inicial` creada y comprobada (§5). El workspace pasa a Pro: 20 $/mes con 20 $ de uso incluido |
| Fase 2: **spike S2.1, resolución territorial** | **Hecho** (2026-09-08, séptima sesión) | `S21TerritoryResolutionSpike` (9 pruebas, red real) + informe `docs/spikes/S2.1-resolucion-territorial.md` + [ADR-011](decisions/ADR-011-resolucion-territorial.md). Desmiente lo que S0.4 y S0.6 daban por sabido: `portalero/v2` es un buscador, no un resolutor (89,5 % de acierto sin poder distinguir el fallo), y `point`/`distance` no filtra. La geometría sí: 1.304 de 1.308 (99,69 %) de acuerdo con `locales-vacios.portal.junta`. Resuelta la correspondencia `distrito.id` ↔ `idpadron` (§5) |
| Fase 2: **módulo `geo`** | **Hecho** (2026-09-08, séptima sesión) | `V008__geo.sql` (`geo_district` con `geometry(Geometry,4326)` e índice GiST, `geo_population_record`), dominio con las dos numeraciones y `DistrictLocator`, `DistrictsIngestionJob` + `DistrictProfileReader` por listener, API `GET /geo/districts`, `/districts/{id}` y `/locate`. `.\mvnw.cmd verify` en verde: **134 tests**, con la comprobación de aceptación de ADR-011. **Integrado en `main` y desplegado** el mismo día (§5) |
| Fase 2: **spike S2.2, ingesta de quejas** | **Hecho** (2026-09-08, octava sesión) | `S22CitizenIngestionSpike` (8 sondas, red real) + informe `docs/spikes/S2.2-quejas-ingesta.md` + [ADR-012](decisions/ADR-012-texto-libre-quejas.md). Responde las cuatro cosas que S0.3 dejó sin comprobar: `fl` recorta de verdad (el texto libre se puede no pedir), el listado tiene **89.432 registros** y es un subconjunto, `updated_datetime` admite FIQL y `sort` (los cierres son capturables), y la cobertura de punto del histórico va del **16 % al 45 %** según el año. Además, los cuatro sinónimos de junta con evidencia (§5) |
| Fase 2: **módulo `citizen`** | **Hecho** (2026-09-08, octava sesión) | `V009__citizen.sql` (sin columna de texto libre), dominio con los cuatro estados de asignación, dos `IngestionJob` con papeles distintos sobre el mismo endpoint (el de altas barre el histórico; el de cierres solo ventanas incrementales, porque por su eje un barrido completo pierde registros), adaptador JDBC con agregaciones (`FILTER`, `percentile_cont`), API `GET /citizen/requests`, `/aggregations` y `/summary` con denominador, cobertura y sin asignar. `geo` gana su superficie pública (`Geo`, `DistrictNames`, `DistrictSummary`). `.\mvnw.cmd verify` en verde: **182 tests**. **Carga completa comprobada contra la API real: 89.432 quejas, las mismas que publica el origen**, y **desplegado en producción** el mismo día con la misma cifra (§5) |
| Fase 1: primera serie de instantáneas observadas | En curso: primer barrido completo el 2026-09-07 (436/436); el 2026-09-08 hay **dos días** de serie y ya se ve movimiento real (§5). Faltan días, no fichas | §4 |
| Fases 3–5 | Pendientes | `spending`, `territory`, frontend, `identity`, `workspace`. De `geo` queda la sección censal (491) como unidad fina opcional; de `citizen`, medir la cobertura real sobre la carga completa (§4) |

Conclusiones que condicionan todo lo demás (siguen vigentes):

- **La unidad territorial es la junta municipal o vecinal (29)**, con la sección censal (491) como opción fina. No existen barrios como dato abierto (S0.4).
- **El gasto público no es territorializable** (S0.2, S0.6, ADR-003). **Pero lo que se cayó fue el dinero, no el territorio**: 323 de 436 fichas (74 %) tienen señal territorial y **la junta de cada punto se resuelve en casa con la geometría oficial** (S2.1 desmintió que la API la resolviera; ADR-011). Siguen en pie cruces con volumen real —`registro-licencia` (42.321 locales, 99 % con punto), `licencia-obra` (2.042 parcelas, 100 %), `via-publica` (100 %), `locales-vacios` (3.824, pero **solo 1.376 con punto, el 36 %, y 1.308 con junta, el 34 %**), quejas (~49 % geolocalizadas)— con padrón y SOCIO24 de denominador. El producto es un observatorio territorial al que le falta una pata, no un proyecto sin eje (SPEC.md §1).
- **La API municipal no resuelve la junta, y lo que el proyecto daba por sabido era falso** (S2.1, ADR-011). El recurso `portal` es un buscador de direcciones que acierta el 89,5 % **sin permitir distinguir el acierto del fallo**, y sus parámetros de consulta espacial no filtran. La asignación territorial se hace con `ST_Contains` sobre los 29 polígonos (99,69 % de acuerdo con la junta oficial), y **sin punto no hay junta**: se cuenta como sin asignar, nunca se geocodifica por dirección.
- **Cuando se puede no recoger un dato personal, no se recoge** (S2.2, ADR-012). El ayuntamiento publica el texto de las quejas sin anonimizar; el 47,9 % de los 7.000 registros muestreados lleva una fórmula de firma y solo el 0,03 % un DNI, así que una redacción por patrones habría quitado lo que una expresión regular sabe encontrar y dejado los ~3.400 nombres. Como `fl` permite no pedirlo, no se pide: la diferencia entre una política de borrado y una ausencia estructural.
- **El monitor de frescura no es una utilidad aparte: es la capa de credibilidad del análisis.** Cuando exista el frontend, la calidad del dato viaja con cada cruce y con su exportación, no en otra pantalla (SPEC.md §1, §3 fase 4). Es lo que distingue esto de un portal de datos.
- **La regla 6 limita la voz del producto, no la inferencia del usuario** (SPEC.md §1.1). Dar cruces, denominadores y series para que el usuario concluya es el objetivo; recortar capacidad de análisis en nombre de la regla 6 es malinterpretarla. Lo difícil no es la conclusión final, que es suya, sino las decisiones editoriales disfrazadas de neutralidad: valores por defecto, clasificación de los mapas, paletas y qué denominadores se ofrecen.
- **La API municipal no honra `If-Modified-Since`** (solo Open311 honra `ETag`); la extensión `.json` es obligatoria; `rows` tope 500 en la sede (S0.5). Codificado en `SourceDescriptor` y `ZaragozaHttpClient`.
- **El 60 % del catálogo no es evaluable por periodicidad declarada** (263/436) y **`modified` no describe el dato en ningún sentido** (S1.1). El monitor publica los dos ejes por separado y sin categoría observada (ADR-005).
- **El catálogo publica servicios que no existen o no son públicos** (S1.1): 8 endpoints API con 404 HTML, 23 distribuciones WFS/WMS de intranet (`-lan`, 403), índices que redirigen (303), una URL con `https:/`. El monitor los muestra como `observationError`.
- **El Swagger documenta la API pero no cambia lo anterior** (S1.2): 60 de 68 fichas con tag casan; 8 tags declarados no existen en el Swagger; 28 tags documentados (OCDS, juntas, líneas de transporte, emisiones…) no tienen ficha. Solo `<declarado>/list` es una alternativa unívoca (4 fichas); el resto de paths del tag no se usa (ADR-006).
- **El listado `catalogo.json` (436) no es todo el catálogo** (S1.3): datos.gob.es lista 369 datasets del ayuntamiento, 261 con ficha en el listado y **108 sin ficha** que existen en el detalle `catalogo/{id}.json` como partes de series y colecciones (`datasetRelacionado IS_PART_OF`). Al menos 544 fichas publicadas. 175 fichas del listado no están federadas (149 `abierto=N`, 15 abiertas, 11 sin valor). El `modified` federado coincide siempre con el municipal.

## 2. Decisiones vigentes

| ADR | Decisión |
|---|---|
| [ADR-000](decisions/ADR-000-especificacion-inicial.md) | `SPEC.md` es la especificación viva y fundacional |
| [ADR-001](decisions/ADR-001-spring-boot-4.md) | Spring Boot 4.1.x, Modulith 2.1.x vía BOM, Java 21 |
| [ADR-002](decisions/ADR-002-spikes-como-tests-junit.md) | Los spikes son tests JUnit `@Tag("spike")`, fuera del build por defecto (`-Pspikes`) |
| [ADR-003](decisions/ADR-003-contexto-spending.md) | El gasto público es el contexto `spending`, sin entidades territoriales |
| [ADR-004](decisions/ADR-004-eventos-jdbc-resiliencia.md) | Registro de eventos Modulith sobre JDBC con tabla creada por Flyway (`ddl-auto=validate`); Resilience4j core programático; ShedLock aplazado; `MockRestServiceServer`; springdoc |
| [ADR-005](decisions/ADR-005-eje-observado.md) | Eje observado: cuatro métodos (`FILE_HEADERS`, `API_MAX_DATE`, `API_COUNT`, `WFS_HITS`) más `NOT_OBSERVABLE`, lista blanca de campos de fecha, orden API → ficheros → WFS, muestreo diario por lotes sin `IngestionJob`, sin categoría observada; `RestClient` único de la aplicación |
| [ADR-006](decisions/ADR-006-inventario-api.md) | Inventario de endpoints: el Swagger se ingiere como documento único (`DOCUMENT`, sin `rows`/`start`) y se sincroniza entero en `catalog_api_endpoint`; cruce por `apiTag` al leer, sin tabla de enlace; la observación solo usa `<declarado>/list` |
| [ADR-007](decisions/ADR-007-federacion.md) | Federación: datos.gob.es paginado por número (`_page`/`_pageSize` ≤ 200, `RESULT_ITEMS`), tabla propia `catalog_federated_dataset` con upsert por página y baja de lo no visto al completar la ingesta (evento); `federated` resuelto al leer por `sourceId`; los 108 federados sin ficha se conservan; las partes de series no se ingieren hasta spike y ADR |
| [ADR-008](decisions/ADR-008-despliegue.md) | Despliegue: imagen propia (`Dockerfile` de la raíz, JRE 21 no root, capas de Boot; los tests no corren en el builder) sobre Railway con la imagen PostGIS `postgis/postgis:17-3.5`; conexión por variables `PG*` sin valor por defecto; una sola réplica (sin ShedLock); en `prod` actuator solo `health`/`info` (**sin `modulith`**) y springdoc visible; infraestructura declarada en `.railway/railway.ts` (IaC, sin secretos), no en el panel ni en el deprecado `railway.json` |
| [ADR-009](decisions/ADR-009-memoria-y-coste.md) | El coste se paga en RAM residente (10 $/GB/mes), no en peticiones: topes de JVM **explícitos** (`-Xmx256m`, `UseSerialGC`, `TieredStopAtLevel=1`) en vez de `MaxRAMPercentage`, que con el límite de 8 GB del plan dejaba el proceso en ~690 MB; medido, baja a 367,8 MB en local y **356 MB** en la instancia. **Serverless no se usa**: el planificador es el producto. Filtrar bots no reduce la factura (sin coste por petición, egress 0) |
| [ADR-010](decisions/ADR-010-copias-de-seguridad.md) | Copias del volumen: **fuera de la IaC** (Railway muestra `backupSchedules` en el `plan`, pero el `apply` no lo aplica ni lo lee), gobernadas por la API pública sobre el id de la **instancia** del volumen; `DAILY` + `WEEKLY` + `MONTHLY`; exigen plan **Pro** y suben el suelo de coste a 20 $/mes; el **PITR no es alternativa** con la imagen PostGIS; una política declarada no es una copia: se verifica con `volumeInstanceBackupList` |
| [ADR-011](decisions/ADR-011-resolucion-territorial.md) | Resolución territorial: la junta se resuelve **en casa** con `ST_Contains` sobre los 29 polígonos, nunca preguntando a la API (`portalero/v2` acierta el 89,5 % sin poder distinguir el fallo; `point`/`distance` no filtra). Sin punto no hay junta: `unassigned`, sin geocodificar por dirección. Dos campos, `districtId` (resuelto) y `districtDeclared` (contraste); `District` con `id` y `padronId`; ambigüedad del solape registrada, no resuelta en silencio |
| [ADR-012](decisions/ADR-012-texto-libre-quejas.md) | El texto libre de las quejas **no se pide**: proyección `fl` fija de ocho campos, sin `address_string` (ADR-011 §2 lo deja sin uso), modelo sin columna donde guardarlo y garantía probada por dos vías. La redacción por patrones descartada con datos; lo que se pierde (búsqueda libre, análisis de temas) se declara |

Nombre del proyecto: `observatorio-zaragoza`; groupId y paquete base `es.zaragoza.observatory`. La carpeta local y el repositorio remoto se llaman `zaragoza-observatorio` (<https://github.com/MarioNaya/zaragoza-observatorio>); no importa para el build.

## 3. Cómo arrancar una sesión

1. Arrancar Docker Desktop y comprobar `docker info` (Testcontainers y Compose lo necesitan). Se puede lanzar desde PowerShell: `Start-Process "C:\Program Files\Docker\Docker\Docker Desktop.exe"` y esperar a que `docker info` responda.
2. `.\mvnw.cmd -v` debe decir Java 21 (el `java` del PATH es Java 8; el wrapper usa `JAVA_HOME`).
3. `.\mvnw.cmd verify`: build completo con PostGIS real (~1,5 min); debe estar en verde antes de tocar nada.
4. Leer `CLAUDE.md` (reglas 1–30) y, para cualquier endpoint, `docs/spikes/README.md` y el informe correspondiente. Nunca escribir un endpoint o campo de memoria.
5. **Mirar el principio de §4**: ahí está lo primero que se hace en la sesión. Si hay un recuadro de tarea inmediata, se borra cuando esté hecho y verificado, no cuando esté planificado.
6. **`main` se despliega solo**: el servicio de Railway construye desde esa rama, así que un push a `main` redespliega la instancia. Razón de más para no romperla. Estado del despliegue: `railway logs --service observatorio --deployment` y `railway metrics --service observatorio`. Ojo: `railway status` y `railway metrics` **salen con código 255 aunque funcionen** (no hay servicio enlazado en la carpeta); hay que mirar la salida, no el código. Antes de invocarla, en PowerShell: `$env:_ = "$env:APPDATA\npm\node_modules\@railway\cli\bin\railway.exe"` (ver `CLAUDE.md`).
7. Trabajo en rama por funcionalidad (`feat/…`), commits pequeños, `main` siempre en verde. `main` integra toda la fase 1, el despliegue, las copias y —desde el 2026-09-08— **S2.1, el módulo `geo`** (merge `0d69ac3`) y **S2.2 más `citizen`** (rama `feat/s22-citizen`), y sigue a `origin/main`. Las seis ramas `feat/*` (`fase1-ingestion-catalog`, `s11-frescura-observada`, `swagger-federated`, `despliegue`, `s21-resolucion-territorial` y `s22-citizen`) **pueden borrarse sin mirar**: `git branch --merged main` las lista todas, ninguna tiene un commit que no esté ya en `main`.

Al cerrar una sesión: `.\mvnw.cmd verify` en verde; actualizar este documento (§1, §4, §5, §6), `SPEC.md` si cambió el modelo o el alcance, y los informes de spikes; pasar la comprobación de datos personales de la regla 22 (`git grep -i -E '\b[0-9]{8}[A-Z]\b|atentamente|set-cookie' -- src/test/resources/fixtures`) y de secretos antes de `git push`; commits descriptivos y push de la rama.

Comandos útiles:

```powershell
.\mvnw.cmd verify                                   # build + tests (Testcontainers), ~1,5 min
.\mvnw.cmd test "-Dtest=CatalogIntegrationTests"    # una clase de test
.\mvnw.cmd test -Pspikes "-Dtest=S13*"              # un spike (red real); refresca fixtures
.\mvnw.cmd spring-boot:run "-Dspring-boot.run.arguments=--server.port=8085"   # app + PostGIS vía Compose
```

El puerto 8080 suele estar ocupado en esta máquina por un contenedor phpMyAdmin de otro proyecto: usar `--server.port=8085` (o parar ese contenedor). No lanzar otro `mvnw` (tests, spikes) con la app arrancada: comparten `target/`. Para parar la app: `Get-NetTCPConnection -LocalPort 8085 -State Listen | Select -Expand OwningProcess | % { Stop-Process -Id $_ -Force }` y después `docker compose -f compose.yaml stop`. Con la app arrancada:

- `GET /actuator/health` → `UP`; `GET /actuator/modulith` lista los módulos.
- A los 30 s el planificador ejecuta los jobs vencidos. El de `geo` trae las 29 juntas con geometría en una petición (~1 s) y su listener lee después el detalle de cada una (29 peticiones, ~9 s): `geo profiles: 29 districts read, 29 with padronId, 116 population records, 0 failed`. Los tres de `catalog`: el catálogo (436 fichas, ~5 s), el Swagger (`sede:catalogo/api`, 1 documento, ~2 s) y la federación (`datos-gob-es:publisher/L01502973`, 369 datasets en 2 páginas, ~5 s); el listener toma las instantáneas del día y da de baja los federados no vistos. A los 2 min empieza el muestreo observado: lotes de 60 fichas cada 10 min (`zaragoza.catalog.observation.*`); para observar todo el catálogo de una vez, añadir `--zaragoza.catalog.observation.initial-delay=PT50S --zaragoza.catalog.observation.batch-size=500 --zaragoza.catalog.observation.request-delay=PT0.3S` (3 min 20 s); para forzar la reobservación de fichas ya observadas hoy, `--zaragoza.catalog.observation.interval=PT1H`.
- `GET /api/v1/geo/districts?sort=padronId,asc` (29 juntas con las dos numeraciones y el padrón del último año), `/api/v1/geo/districts/6` (El Rabal con su serie), `/api/v1/geo/locate?lon=-0.8657195889873618&lat=41.65695446364446` (→ `RESOLVED` junta 6).
- `GET /api/v1/citizen/summary`, `/api/v1/citizen/requests?size=5&assignment=no_point`, `/api/v1/citizen/aggregations?by=district` (padrón, año, cobertura de punto y sin asignar), `?by=category`, `?by=month`. **Ojo con la primera ejecución**: el job de altas de `citizen` barre el listado entero (89.432 registros, 179 páginas, **~6 min** medidos). El de cierres no: arranca desde el momento en que se ejecuta y resuelve su ventana en una página (60 registros en 2 s en la carga limpia del 2026-09-08), porque por su eje un barrido completo perdería registros (§5). A partir de ahí los dos piden solo lo nuevo.
- `GET /api/v1/catalog/summary` (incluye `apiInventory` y `federation`), `/api/v1/catalog/datasets?size=5&sort=observedLastChange,desc`, `/api/v1/catalog/datasets?federated=false&open=true`, `/api/v1/catalog/datasets/132` (ficha observada por `asociacion/list`, con `apiEndpoints`), `/api/v1/catalog/api-tags`, `/api/v1/catalog/api-endpoints?tag=Urbanismo:%20Clavos%20Topograficos`, `/api/v1/catalog/federation?inCatalog=false`.
- Contrato OpenAPI en `/v3/api-docs`; Swagger UI en `/swagger-ui.html`.
- La base de datos de Compose usa el volumen `postgres-data` (persiste entre arranques; V008 ya aplicada); `docker compose -f compose.yaml stop` para pararla. Para consultas SQL: `docker exec -i zaragoza-observatorio-postgres-1 psql -U observatorio -d observatorio < fichero.sql` (desde Git Bash, la ruta dentro del contenedor se pasa por stdin para evitar la conversión de rutas).

## 4. Siguiente paso: lo que queda de la fase 1 y la fase 2

La fase 1 está completa, desplegada y respaldada; lo que le queda es tiempo. **La fase 2 ya tiene hechos sus dos primeros pasos (S2.1 y el módulo `geo`) y lo primero de la próxima sesión es `citizen`** (más abajo, tras la lista de vigilancia). Lo de la fase 1 se vigila, no se trabaja — salvo el punto 6, que sí es trabajo y es nuevo:

1. **Primera serie de instantáneas observadas.** No hay nada que hacer todavía, solo dejar pasar los días. El **primer barrido completo terminó el 2026-09-07 (436 de 436 fichas)**; a partir de ahí la instancia reobserva cada ficha una vez al día, así que lo que falta es acumular *días*, no fichas: una sola foto no es una serie. El 2026-09-08 hay dos días y ya se ve movimiento real (§5). **Se mira cuando haya dos o tres semanas**, no antes; entonces habrá con qué decidir: si existe una categoría observada y cómo se cruza con la declarada (ADR-005 §6, ADR propia), los umbrales `zaragoza.catalog.freshness.*` y la retención de `raw_payload`. También dirá si el Swagger y la federación cambian (hoy no hay marca de cambio en ninguna de las dos fuentes; `firstSeenAt`/`lastSeenAt` son la única serie). Se mira con `GET /api/v1/catalog/datasets/{id}/freshness-history` y, para el conjunto, por SQL sobre la base de producción.
2. ~~Copias del volumen~~ → hecho y verificado el 2026-09-08 (ADR-010). **Lo que queda abierto de esto**: la restauración no se ha ensayado nunca, y todas las copias viven en Railway; si el riesgo de proveedor llega a importar, la vía es un volcado lógico periódico fuera de Railway (ADR-010, alternativas). Conviene mirar de vez en cuando que las programadas aparecen de verdad: `volumeInstanceBackupList` (`docs/despliegue.md` §7).
3. **Limpieza pendiente en el volumen de producción**: el clúster de PostgreSQL 18 que Railway inicializó por defecto sigue en `/var/lib/postgresql/data/pgdata`, inservible pero intacto (ADR-008 §8). Se conservó por prudencia; se puede borrar cuando conste que la instancia va bien.
4. **Vigilar el coste y la memoria** (ADR-009, ADR-010): `railway metrics --service observatorio`. ~~Pendiente concreto de la próxima sesión~~ → **medido el 2026-09-08** (ADR-009, adenda, y §5). La hipótesis que había anotada —que los 407 MB tras `geo` eran presión de **metaspace**— **es falsa**: con `geo` y `citizen` dentro y la carga completa de 89.432 quejas hecha, el metaspace se queda en **110,1 MB de un tope de 192** y el heap no pasa de 136,6 MB de 256. Bajar `MaxMetaspaceSize` no recuperaría nada y sí acercaría un `OutOfMemoryError`. Lo que hay es un crecimiento **real y modesto**: de 367,8 MB con dos módulos a **388,8 MB con cuatro**, unos 21 MB repartidos entre metaspace, code cache y páginas residentes. **Ninguna bandera se ha tocado.** La conclusión práctica es que el umbral de 400 MB se fijó para una aplicación de dos módulos y ya no sirve de alarma: lo que se vigila es un salto respecto de la línea base de la configuración actual (~389 MB en local), no el cruce de una raya puesta para otra cosa. En dinero no cambia nada: a 10 $/GB/mes son ~4,4 $/mes y el proyecto sigue muy dentro del crédito de 20 $ del plan Pro. La lectura equivalente en la instancia, ya con `citizen` desplegado y cargado: **416 MB** (371 recién arrancada, 401–407 durante la carga). Esa es la **línea base de producción con los cuatro módulos**, y contra ella se comparan las siguientes. Ojo: el crédito y el límite de gasto son **de la cuenta, no del proyecto**, y hay otra aplicación en la misma cuenta.
5. ~~Primer despliegue con migración nueva~~ → **hecho el 2026-09-08 con `V008`** y sin incidencias (§5): Flyway validó las 8 migraciones y aplicó la nueva en 78 ms sobre la base de datos de producción, la aplicación arrancó en 6,73 s y los datos anteriores siguen ahí. Ya no es una incógnita, pero la precaución sigue valiendo para la siguiente: con `ddl-auto=validate` un fallo de migración aborta el arranque.

6. **Las fichas que desaparecen del catálogo no se ven** (detectado el 2026-09-08). El listado municipal pasó de 436 a 434 fichas y el observatorio siguió diciendo 436: `RegisterDatasets` hace upsert y nunca marca ni da de baja lo que deja de aparecer, mientras que la federación sí lo hace (`purgeNotSeenSince`, S1.3). Hay `firstSeenAt`/`lastSeenAt` en la tabla, así que el dato está: falta exponerlo. **No se borra la ficha** —su histórico de frescura es justamente lo valioso—: lo razonable es un filtro y un recuento de «ya no listadas» en `summary`, y decidirlo con una ADR corta, porque «desaparecida del catálogo» es una afirmación sobre el publicador (regla 6). Conviene mirarlo pronto, mientras se sepa qué dos fichas son.

Mejoras menores, solo si hacen falta: `apiDefinition`/`parameters` del Swagger para conocer los campos ordenables sin sondear; `fl=<campo>` en el `sort`; `ETag`/`Content-Length` como firma de cambio para ficheros sin `Last-Modified` (2 de 72); `produces` del Swagger para saber qué endpoints tienen `application/geo+json`.

La **fase 2** tiene hechos sus tres pasos y su orden fue el correcto: primero saber si la API resolvía la junta (no), después el territorio, después la fuente.

1. ~~Spike S2.1: resolución dirección/punto → junta~~ → **hecho el 2026-09-08** (`docs/spikes/S2.1-resolucion-territorial.md`, ADR-011). Era la llave del eje territorial y cambió el plan: **la API no resuelve la junta**. La vía buena es la geométrica y está probada (99,69 % de acuerdo con la junta oficial sobre 1.308 registros). También quedó resuelta la correspondencia `distrito.id` ↔ `idpadron`.
2. ~~Módulo `geo`~~ → **hecho el 2026-09-08**. `V008`, `District` con las dos numeraciones, `DistrictLocator` por `ST_Contains`, la capa base como `IngestionJob` y el detalle de cada junta por listener. Desde la octava sesión tiene además **superficie pública** (`Geo`, `DistrictNames`, `DistrictSummary`): es lo que consume `citizen` sin tocar sus tablas.
3. ~~Spike S2.2 y módulo `citizen`~~ → **hechos el 2026-09-08** (`docs/spikes/S2.2-quejas-ingesta.md`, ADR-012). La ADR sobre el texto libre fue antes que el código, como estaba previsto, y la respuesta resultó mejor que las tres opciones que SPEC.md §9 barajaba: no hay que decidir cómo guardarlo porque **se puede no pedirlo**. Con `geo` en pie, cada queja guarda `districtId` y `districtDeclared`, y la tabla de sinónimos existe ya con sus cuatro entradas.

**Lo que queda abierto de `citizen`, y es lo primero de la próxima sesión:**

1. ~~Medir la cobertura territorial sobre la carga completa~~ → **medida el 2026-09-08** (§5): 29,1 % del total, con 86,5 % en 2013, 18–33 % de 2014 a 2024, 37,4 % en 2025 y 49,7 % en 2026. **Lo que queda es decidir, no medir**: si las series por junta y año se ofrecen de alguna forma comparable —haría falta ADR, porque cualquier ajuste por cobertura es una conclusión (regla 6)— o si basta con lo que ya se hace, que es publicar la cobertura al lado de cada cifra.
2. **Decidir qué hacer con los servicios `INTERNAL`** (~2 % de los registros, S0.3 y S2.2): no son quejas ciudadanas. Hoy cuentan y se pueden filtrar por su código; excluirlos en silencio sería una decisión editorial encubierta (regla 6), así que si se excluyen, con ADR corta.
3. **Mirar la discrepancia entre junta resuelta y declarada** ahora que hay 89.432 registros para medirla. Es un dato publicable sobre la calidad de la fuente y ya está en `summary`; lo que falta es leerlo.
4. **Contrastar con Open311** (S0.3 lo dejó como fuente de contraste): sirve para saber si el listado de sede omite registros que Open311 sí publica, que es la parte más incómoda de la duda sobre el criterio de publicación.
5. **Vigilar el flanco que deja el reparto de los dos jobs** (S2.2 §10): el de cierres solo es seguro mientras su ventana quepa en una página, porque por el eje de `updated_datetime` la paginación por offset pierde registros. Con ~46 cierres al día sobra, pero tras varios días de parada de la instancia la ventana crecería. La salida, si llega a hacer falta, es trocear la ventana por fechas —no un desempate secundario, que la API acepta sin que se pueda saber si lo aplica—. Conviene comprobarlo la primera vez que la instancia esté parada más de un día.

Después de eso, la fase 3 (`spending`) o el siguiente cruce territorial (`registro-licencia`, `locales-vacios`).

**Sobre el ritmo**: cada fuente nueva son spike, fixtures, ADR, traductor, migración y tests. Cuatro fuentes han costado seis sesiones a esta calidad. Es preferible **dos o tres cruces bien hechos** —`registro-licencia`, `locales-vacios` y quejas, los tres con padrón, ya dan un producto defendible— que ocho fuentes a medias.

Y, con spike propio, la **ingesta de las partes de series y colecciones** que el listado `catalogo.json` omite (SPEC.md §9, S1.3): al menos 108 fichas federadas solo alcanzables por `catalogo/{id}.json`.

## 5. Comprobaciones reales (19:01, 22:27, 23:12 CEST del 2026-09-06; 00:26 del 2026-09-07; 10:30, 11:47, 12:23, 13:33, 16:30, 17:10 y 17:40 del 2026-09-08)

**17:40 CEST del 2026-09-08 (octava sesión, `citizen` desplegado y cargado en producción).** Merge `e9433bf` a `main` y push: Railway redesplegó solo (`2d23cb4c`, SUCCESS). Segundo despliegue con migración nueva y, otra vez, sin incidencias:

- `Successfully validated 9 migrations` → `Migrating schema "public" to version "009 - citizen"` → **aplicada en 37 ms**; la aplicación arrancó en **6,011 s**.
- **Sin regresiones en lo anterior**: `catalog/summary` sigue en 436 fichas, `geo/districts` en 29 juntas y `geo/locate` resuelve El Rabal como antes.
- **La carga en producción cuadra exactamente con el origen: 89.432 quejas**, de 2013-01-08 a 2026-09-08. Por estado: 81.963 cerradas, 7.468 abiertas, **1 rechazada** y 0 desconocidas —es decir, `REJECTED` ya está reconocido y no queda ningún valor sin traducir—.
- **Territorio**: 26.056 `RESOLVED`, 13 `AMBIGUOUS` (el solape de Juslibol, que aparece solo en los datos reales), 50 `OUTSIDE` y 63.313 `NO_POINT`. La agregación por junta lo devuelve cuadrado: **26.069 asignadas y 63.363 sin asignar**. Contraste con lo declarado: 20.294 coinciden, 1.210 discrepan y **0 nombres sin casar**.
- **Memoria**: 371 MB recién arrancada, 401–407 MB durante la carga y **416 MB** al terminarla. La predicción hecha con la medida local (~420-440 MB) se cumple, y ahí queda la **línea base de producción con los cuatro módulos**, que es lo que faltaba de §4 punto 4. Sigue costando ~4,2 $/mes de memoria, muy dentro del crédito de 20 $ del plan Pro.

**17:10 CEST del 2026-09-08 (octava sesión, memoria con la imagen de producción).** La medición que §4 punto 4 dejaba pendiente, hecha con `compose.prod.yaml` —misma imagen, mismas banderas, perfil `prod`— sobre la carga completa de `citizen` más un lote de 60 observaciones. Para ver el desglose por área se expuso `/actuator/metrics` **solo en esa ejecución local**, con un override de Compose fuera del repositorio: el perfil `prod` versionado sigue exponiendo únicamente `health` e `info` (ADR-008 §6).

| Momento | RSS | Heap | Metaspace | Clases |
|---|---|---|---|---|
| Arranque, sin ingesta | 330,1 MB | 109,5 MB | 100,4 MB | 22.332 |
| Durante la carga de 89.432 quejas | 356 → 384 MB | 86–137 MB | 107,2 MB | 23.8xx |
| Al terminar la carga (179 páginas) | 384,3 MB | 136,6 MB | 110,0 MB | 24.315 |
| Tras el primer lote de observación (60 fichas) | 390,0 MB | 92,1 MB | 110,1 MB | 24.343 |
| **Estabilizada** | **388,8 MB** | 92,7 MB | 110,1 MB | 24.343 |

**La hipótesis del metaspace era falsa**: 110,1 MB de un tope de 192 (57 %), y el heap no pasa de 136,6 de 256. Los dos módulos nuevos añaden ~10 MB de metaspace, no lo llenan. El crecimiento real es de **367,8 MB con dos módulos a 388,8 MB con cuatro**, unos 21 MB. Ninguna bandera tocada. Detalle y consecuencias en la adenda de ADR-009.

**16:30 CEST del 2026-09-08 (octava sesión, `citizen` contra la API real en local).** La ingesta completa de quejas, ejecutada de verdad. Es donde aparecieron las dos cosas que ningún test había visto:

- **V009 aplicada y arranque normal**: `Migrating schema "public" to version "009 - citizen"`, aplicación en pie en **12,16 s** con los datos anteriores intactos.
- **Primera ejecución, con dos fallos encadenados.** El job de cierres corrió primero (orden de beans), barrió el listado entero por `updated_datetime asc` —179 páginas, **89.432 registros declarados**— y dejó solo **80.628 filas**. Ningún registro se descartó al traducir: la diferencia era del propio recorrido. Y a continuación el job de altas encontró la tabla poblada, aplicó la marca de agua y trajo **25 registros en vez de 89.432**.
- **Diagnóstico, barriendo el listado entero por cada eje y contando ids distintos**: por `requested_datetime asc`, 89.432 filas y **89.432 ids distintos**; por `updated_datetime asc`, las mismas filas y solo **80.628 ids** —2.680 repetidos, alguno siete veces, y **8.804 que no aparecen nunca**—. Las quejas se cierran por lotes, comparten `updated_datetime` y entre filas empatadas el orden no es estable entre páginas. **Que dos páginas consecutivas no solapen no prueba que un barrido esté completo** (S2.2 §10).
- **Corregido y vuelto a comprobar desde cero** (tabla vaciada y ejecuciones borradas): el job de cierres resolvió su ventana en **1 página, 60 registros, 2 s**, y el de altas hizo el barrido completo: **89.432 registros en 179 páginas, ~6 min**. En la tabla, **89.432 filas**: la carga cuadra exactamente con el origen.
- **Y un tercer estado que ninguna muestra vio**: la queja 412792 (2014) viene con `status=rejected`, valor que no aparece en los 7.000 registros que muestreó S2.2. No se perdió ni tumbó nada porque el traductor marca como `UNKNOWN` lo que no reconoce; ahora `REJECTED` está reconocido y `UNKNOWN` sigue ahí para el siguiente. De propina, dos rarezas del origen: `quejas-sugerencias/412792.json` responde **400 con `"Index: 0, Size: 0"`** (un error interno filtrado al exterior) mientras el listado sirve ese mismo registro sin problema, y **`status=rejected` como filtro se ignora en silencio** y devuelve cerradas.
- **Datos de la carga completa** (89.432 registros, 2013-01-08 → 2026-09-08): 81.963 cerradas, 7.468 abiertas, 1 rechazada. Territorio: **29,1 % con junta resuelta** (26.056), 70,8 % sin punto, 50 fuera del término y 13 ambiguas por el solape de Juslibol. Contraste declarado/resuelto: **20.294 coinciden y 1.210 discrepan** (5,6 %), y ningún nombre declarado se quedó sin casar, así que los cuatro sinónimos de `DistrictNames` cubren todo lo que publica la fuente. La tabla ocupa **23 MB**.
- **Cobertura de punto por año, ya sobre la carga completa** y no sobre muestra: 86,5 % en 2013, entre 18 % y 33 % de 2014 a 2024, 37,4 % en 2025 y **49,7 % en 2026**. Confirma el orden de magnitud que midió S2.2 con muestra sesgada y refuerza el caveat: dos años no son comparables sin mirar su cobertura.
- **API con datos reales**: `summary` cuadra con SQL; `aggregations?by=district` devuelve las 29 juntas con su padrón de 2024, sus quejas por mil habitantes, su cobertura y su mediana de respuesta, más 63.363 sin asignar al lado. El caso extremo que el caveat de `geo` anunciaba aparece solo: Torrecilla de Valmadrid, 2 quejas y 20 habitantes, sale a 100 por mil.


**13:33 CEST del 2026-09-08 (séptima sesión, `main` desplegada con migración nueva).** Merge `0d69ac3` a `main` y push: Railway redesplegó solo. **Es el primer despliegue que trae una migración Flyway**, que era la incógnita del punto 5 de §4, y salió limpio:

- `Successfully validated 8 migrations` → `Migrating schema "public" to version "008 - geo"` → `Successfully applied 1 migration … (execution time 00:00.078s)`. La aplicación arrancó en **6,73 s** y los datos anteriores (catálogo, instantáneas, inventario, federación) siguen intactos.
- A los 30 s, `ingestion … started for sede:distrito`; a los 45 s la API pública ya devolvía las 29 juntas.
- Comprobado en <https://observatorio-production-ed20.up.railway.app>: `GET /api/v1/geo/districts?sort=padronId,asc` → 29 juntas con las **dos numeraciones** (Casco Histórico `id` 3 / `padronId` 1, Centro 4/2, Delicias 5/3, La Almozara 2/4) y el padrón de 2024; `/geo/districts/6` → El Rabal, `padronId` 15, años 2024, 2022, 2021 y 2020; `/geo/locate` con el punto de un local real de El Rabal → `RESOLVED` junta 6, con Madrid → `OUTSIDE` y con un punto del norte rural (−0,99088949, 41,73899086) → **`AMBIGUOUS`** con candidatas `[14, 18]`, Alfocea y Juslibol: el solape que midió S2.1, funcionando como decidió ADR-011. Ese caso quedó además cubierto por `GeoIntegrationTests`. `health` UP, **`/actuator/modulith` sigue en 404** (ADR-008) y `/v3/api-docs` pasa de 7 a **10 rutas**.
- **Memoria**: tres lecturas de `railway metrics` a los 2, 6 y 12 minutos del despliegue dan **396, 405 y 407 MB**, con un máximo de 824 MB en la hora que corresponde al solape de contenedor viejo y nuevo durante el redespliegue. No es un pico de arranque: **se ha asentado unos 50 MB por encima de la referencia de 356 MB** de ADR-009. Queda como tarea medida, no como sospecha (§4 punto 4).

**12:23 CEST del 2026-09-08 (séptima sesión, `geo` contra la API real).** Aplicación en el puerto 8085 con la base de datos de Compose (venía de la sesión anterior): Flyway aplicó `V008` sin incidencias sobre datos existentes —el primer estreno local de una migración nueva— y a los 30 s el planificador ejecutó los cuatro jobs. `ingestion … succeeded for sede:distrito: 29 records in 1 pages` (0,96 s) y, 9 s después, el listener: **`geo profiles: 29 districts read, 29 with padronId, 116 population records, 0 failed`** (29 × 4 años). Comprobado por la API:

- `GET /geo/districts?sort=padronId,asc` → 29 juntas con las **dos numeraciones**: Casco Histórico `id` 3 / `padronId` 1, Centro 4/2, Delicias 5/3, La Almozara 2/4. Es exactamente el desajuste que S2.1 midió, ahora servido como dato.
- `GET /geo/districts/6` → El Rabal, `padronId` 15, serie 2024 (78.438 hab, 9.337,7 hab/km²), 2022, 2021 y 2020. **Sin 2023.**
- `GET /geo/locate` con el punto de un local real de El Rabal (−0,86572, 41,65695) → `RESOLVED` junta 6, que es la que le asigna el ayuntamiento; con Madrid (−3,7038, 40,4168) → `OUTSIDE`; y con el local 823 del borde (−0,902619, 41,655046) → `La Almozara`, la discrepancia conocida con la junta oficial (Delicias).
- **La comprobación de aceptación de ADR-011 está automatizada** (`GeoIntegrationTests`): sobre la página grabada de `locales-vacios`, `ST_Contains` en PostGIS da **233 aciertos de 237**, 0 puntos fuera, 0 ambiguos y las 4 discrepancias son todas `Delicias → La Almozara`. Es registro a registro lo mismo que midió el ray casting del spike.
- De paso, un dato del catálogo: la ingesta trajo **434 fichas**, dos menos que las 436 de los últimos cuatro días. **Y eso destapa un hueco**: `summary` sigue diciendo 436 porque `catalog_dataset` solo hace upsert y nunca da de baja lo que desaparece del listado —a diferencia de la federación, que sí purga lo no visto (S1.3)—. Las dos fichas que se han caído siguen ahí con su `lastSeenAt` antiguo y nada en la API lo dice. Es la primera vez que el catálogo encoge, así que es la primera vez que se nota. Ver el punto 6 de §4.

**11:47 CEST del 2026-09-08 (séptima sesión, S2.1).** Spike `S21TerritoryResolutionSpike` contra la API real, 9 pruebas en verde. Lo esencial, con el detalle en `docs/spikes/S2.1-resolucion-territorial.md`:

- **`portalero/v2/list` no es un resolutor.** Sin filtro no devuelve `result`; `totalCount` siempre `-1`; la `junta` llega **sin `id`** y `CASCO HISTÓRICO` trae un carácter no imprimible (U+0093); `q=junta.id==6` devuelve portales de Garrapinillos y `q=junta.id==99` devuelve «AVENIDA NAVARRA, 99»; `direccion=CALLE INEXISTENTE 999` devuelve «AVENIDA MONTAÑANA, 999». Sobre 19 direcciones reales acierta la junta en 17 (**89,5 %**) **sin que se pueda distinguir el acierto del fallo**.
- **No hay consulta espacial**: dos puntos separados 12 km devuelven el mismo registro con `distance=50`; `point` sin `distance` da 400 (`"mensaje":"500 "`) y `registro-licencia/portal` con `point` da 400 filtrando un `SQLGrammarException` de Hibernate.
- **La geometría sí resuelve**: punto-en-polígono sobre las 29 juntas contra `locales-vacios.portal.junta` → **1.304 de 1.308 (99,69 %)**, 0 puntos fuera y 0 en dos juntas; las 4 discrepancias, todas en el mismo tramo del borde Delicias/Almozara. Segunda opinión con `edificio-historico`: 491/496 (99,0 %).
- **Numeraciones resueltas**: `iddatosab` == `distrito.id` en las 29 e `idpadron` es la otra numeración, estable; vienen juntas en `distrito/{id}.indicadores`. Padrón 2020, 2021, 2022 y 2024 (**falta 2023**); de 101.521 habitantes (Delicias) a 20 (Torrecilla de Valmadrid).
- **Cobertura por fuente** (muestra de 500): `licencia-obra` 100 %, `via-publica` 100 %, `registro-licencia` 99,0 %, `edificio-historico` 99,4 %, **quejas 49,2 %**, **locales-vacios 48,8 %** (corrige S0.6: no son 3.824 geolocalizados, sino un tercio), `asociacion` 0 % con punto pero 99,6 % con junta declarada.
- **Topología**: los 29 polígonos **no son una partición** (0,29 % de la superficie cubierta cae en dos juntas, siempre con Juslibol) y hay nombres que no casan: `DISTRITO SUR`, `SAN JUAN DE MOZARRIFAR`.

**10:30 CEST del 2026-09-08 (sexta sesión, copias y serie).** La instancia sigue viva y sola: `summary` con `ingestedAt` 2026-09-08T04:30:33Z, `latestSnapshotOn` 2026-09-08, **`withoutSnapshot` 0 y `withoutObservation` 0** — las 436 fichas observadas otra vez, sin intervención. El reparto por método se mueve un poco entre días (`FILE_HEADERS` 73, `API_MAX_DATE` 89, `API_COUNT` 47, `WFS_HITS` 76, `NOT_OBSERVABLE` 151), lo que ya es una señal: la observabilidad de una ficha no es estable.

**La serie empieza a decir cosas.** Dos días de historia en `freshness-history`, y en el primero que se mira ya hay movimiento real: «Censo de Asociaciones» (132) pasa de 2.823 registros y `creationDate` máximo 2026-07-26 (instantánea del 07) a **2.827 registros y 2026-08-16** (instantánea del 08). Es exactamente lo que el eje observado existe para ver, y lo que la periodicidad declarada de esa ficha (`NOT_EVALUABLE`) no dice.

**Copias del volumen (ADR-010).** Antes: `volumeInstanceBackupScheduleList` y `volumeInstanceBackupList` vacíos, ninguna copia desde el despliegue. La ruta prevista —declararlo en `.railway/railway.ts`— resultó ser un callejón: `railway config plan` muestra el cambio, pero `apply` responde `status: "applied"` con `changes: []` y no toca nada (probado con la CLI 5.49.2, con la 5.49.5 y con plan fijado `--out`/`--plan`), y tampoco lee el estado real: con el calendario ya activo, el `plan` seguía anunciando `null → ["DAILY"]`. En Hobby, además, la mutación devolvía `Not Authorized`: las copias y el PITR son de plan Pro. Con el workspace en Pro, por la API pública:

| Calendario | Cron (UTC) | Retención |
|---|---|---|
| `DAILY` | `51 16 * * *` | 518 400 s = 6 días |
| `WEEKLY` | `39 11 * * 6` | 2 332 800 s = 27 días |
| `MONTHLY` | `4 8 1 * *` | 7 689 600 s = 89 días |

Y una copia real, no una promesa: `verificacion-inicial`, creada a las 08:43:22Z, `referencedMB` 334, sin caducidad; tardó segundos, sin redespliegue ni corte. El PITR quedó descartado por la propia CLI: `railway postgres pitr status --service postgis` responde que corre sobre las imágenes de base de datos de Railway y que `postgis/postgis:17-3.5` no es una de ellas.

**00:26 del 2026-09-07 (quinta sesión, instancia desplegada).** <https://observatorio-production-ed20.up.railway.app>. `health` UP; `/actuator/modulith` **404**; `/v3/api-docs` con OpenAPI 3.1.0 y 7 rutas; `summary` con **436 fichas, 497 operaciones en 84 tags y 369 federados** (261 con ficha, 108 sin ella), `ingestedAt` 2026-09-06T22:26:43Z. El muestreo observado arrancó con su primer lote de 60 fichas (`FILE_HEADERS` 11, `API_MAX_DATE` 17, `API_COUNT` 15, `WFS_HITS` 1, `NOT_OBSERVABLE` 16; 376 sin observar). Las mismas cifras que en local, ahora desde la nube.

**Memoria, el mismo día (ADR-009).** Con la instancia arriba y sin uso real (10 peticiones en una hora, egress 0 MB), `railway metrics` daba **688 MB** en `observatorio` y ~125 MB de media en `postgis`. La causa era `-XX:MaxRAMPercentage=75.0` en el `Dockerfile`: Railway anuncia el límite del plan (8 GB), así que la JVM se creía con derecho a ~6 GB de heap. Con topes explícitos, medido primero en local sobre una ingesta completa más un lote de observación (**367,8 MB estables** durante siete minutos, sin OOM) y después en la instancia: **356 MB**. El proyecto queda en ~5,7 $/mes (3,6 la app + 1,25 `postgis` + 0,75 el volumen de 5 GB).

El despliegue costó tres intentos, y los dos fallos están documentados en ADR-008 §8 porque volverían a morder: (1) declarar la base con `database(...)` e imagen propia genera un bucle de borrar y recrear —hay que declararla como `service` con `image(...)`—; y (2) Railway inicializa el volumen con **Postgres 18**, sobre el que PostgreSQL 17 no arranca (`unrecognized configuration parameter "autovacuum_worker_slots"`), así que `PGDATA` va a un directorio propio.

**23:12 (quinta sesión, imagen de producción).** `docker compose -f compose.prod.yaml up --build` con PostGIS efímero y proyecto aislado: imagen construida desde cero con el wrapper del proyecto; Flyway aplicó las 7 migraciones incluida la extensión PostGIS; la aplicación arrancó en 14,6 s y respondió `health=UP` a los 8 s. Comprobado dentro del contenedor: proceso como `uid=10001(observatory)` (no root) y `TZ=Europe/Madrid`. `/actuator` expone solo `health` e `info`; **`/actuator/modulith` devuelve 404** (ADR-008 §6); `/v3/api-docs` sirve OpenAPI 3.1.0 con 7 rutas; los logs salen en ECS JSON. A los 30 s los tres jobs ingirieron contra las fuentes reales desde la imagen de producción: **436 fichas, 497 operaciones en 84 tags y 369 federados (261 con ficha, 108 sin ella)**, las mismas cifras que los dos arranques anteriores. Se paró antes de los 2 min para no lanzar el muestreo observado contra la API municipal sin necesidad.

Cuarta sesión, aplicación en el puerto 8085 con PostGIS de Compose (base de datos de las sesiones anteriores). Dos arranques:

**19:01 (Swagger y observación).** Flyway aplicó `V006` sin incidencias; a los 30 s `ingestion … succeeded for sede:catalogo/api: 1 records in 1 pages` (1,8 s, 1,3 MB) y el listener registró el documento; el catálogo no se reingirió (última ingesta a las 17:51, intervalo 6 h). API: `summary.apiInventory` = 497 operaciones, 84 tags, 68 fichas con tag, 60 documentadas, 28 tags sin ficha; `api-tags` devuelve 92 entradas (84 del Swagger más los 8 tags declarados sin documentar: Convenios, Cuentas Bancarias, Facturas, Periodo Medio de Pago, Entornos BIC, Puntos wifi, Solares, Mapas colaborativos) y los 3 tags con varias fichas (Agenda Zaragoza [282, 2700], Presupuestos [336, 2200, 2201], Transporte urbano [327, 335]); el detalle de la 13 lista sus 10 operaciones con `tagDocumented=true`, el de la 79 (Puntos WIFI) `tagDocumented=false`. Con `initial-delay=PT50S`, `batch-size=500`, `request-delay=PT0.3S` e `interval=PT1H`, el muestreo reobservó **las 436 fichas en 3 min 20 s**: `observed 436 datasets (243 with a measure)` (antes 240).

| Método (última observación) | Fichas | Con fecha | Con recuento | Con error |
|---|---|---|---|---|
| `API_MAX_DATE` | 91 (+3) | 91 | 89 | 0 |
| `API_COUNT` | 46 | — | 31 | 15 |
| `FILE_HEADERS` | 72 | 69 | — | 3 |
| `WFS_HITS` | 76 (−1) | — | 52 | 24 |
| `NOT_OBSERVABLE` | 151 | — | — | — |

- La regla `<declarado>/list` (ADR-006) actuó en las cuatro fichas previstas: «Censo de Asociaciones» (132) pasó de 303 a `API_MAX_DATE` por `asociacion/list` (2.823 registros, `creationDate` 2026-07-27); «Clavos Topográficos» (247) pasó de `WFS_HITS` a `API_MAX_DATE` por `clavo-topografico/list` (4.357, `lastUpdated` 2017-12-29); «Premios y Concursos» (1760) de objeto sin lista a `API_MAX_DATE` por `premios-concursos/list` (32, `lastUpdated` 2026-08-12); «Artistas y Creadores» (1920) de «respuesta no JSON» a `API_COUNT` por `artista-creador/list` (759, sin campo de fecha admitido).
- 42 intentos fallidos (antes 45): 23 × 403 de intranet, 6 × 404 HTML, 3 × 303 (`aplicacion`, `catalogo`, `presupuesto`), 2 × 400, 2 ficheros sin `Last-Modified`, 3 objetos sin lista (`convenios`, `cultura`, `calidad-aire`), 1 URL `https:/`, 1 TLS del Catastro, 1 respuesta no JSON.

**22:27 (federación).** Flyway aplicó `V007`; a los 30 s `ingestion … succeeded for datos-gob-es:publisher/L01502973: 369 records in 2 pages` (5 s) y el listener: `federation run … listed 369 datasets; 0 no longer federated`. API: `summary.federation` = 369 federados, 261 con ficha, 108 sin ficha, 175 fichas sin federar; `federation?inCatalog=false` devuelve los 108 (279 «Vías», 307, 1880 «Mapa Estratégico de Ruido 2016», 2720, 3060 «Portalero», … 6201 «Carril Bici 2018», 6202, 6240 «Calidad del aire: Histórico completo»); «Callejero» (22) `federated=true` con su URL en datos.gob.es. En base de datos, las 175 no federadas se reparten en 149 `is_open=false`, 15 `true` y 11 `null`, como en el spike; y de los padres declarados por las partes de series, 22 «Callejero» y 131 «Calidad del Aire» están en el listado y federados, mientras que 16 «Carril Bici» y 2940 «Temperatura Ambiental e Islas de calor» no están en ninguna de las dos fuentes (la jerarquía tiene fichas que nadie lista).

## 6. Pendientes del usuario

- **Vigilar el coste de Railway** y, ahora en concreto, **la memoria tras el despliegue de `geo`**: se ha estabilizado en **~407 MB** frente a la referencia de **356 MB** de ADR-009, por encima del umbral de 400 MB que esa ADR marca como señal de regresión. Las banderas de la JVM no se han tocado. Con el plan Pro no cambia la factura mientras quepa en el crédito, pero sí el margen; hay que medirlo y decidir (§4 punto 4). Hay dos servicios y un volumen de 5 GB corriendo de forma continua, y desde el 2026-09-08 el plan es **Pro** (20 $/mes con 20 $ de uso incluido), que se paga por las copias del volumen (ADR-010). El consumo del proyecto es ~5,7 $/mes, así que cabe de sobra en el crédito; lo que conviene mirar es **la cuenta entera**, porque el crédito es de la cuenta y hay otra aplicación en ella. Si algún día el proyecto deja de justificar 20 $/mes, la alternativa documentada es volver a Hobby y sustituir las copias por un volcado lógico fuera de Railway (ADR-010, alternativas).
- **Elegir licencia del repositorio**: hoy no hay `LICENSE`, y un repositorio público sin licencia es técnicamente «todos los derechos reservados» — lo contrario del mensaje del proyecto. Es una decisión tuya (MIT, Apache-2.0, AGPL…), no técnica.
- **Integración continua**: no existe `.github/workflows`. El build solo está verde porque se lanza a mano. Un workflow que ejecute `./mvnw verify` en cada push es barato y, en un repositorio público, se lee de un vistazo.
- **Reiniciar Claude Code** para que registre el MCP de Railway, ya configurado (`C:\Users\mario\.claude.json`). Sigue sin conectar: el 2026-09-08 fallaba con `CONNECTION_CLOSED` **con la sesión de la CLI válida** (`railway whoami` correcto), así que no era cuestión de login. No bloquea nada —la CLI cubrió todo, incluidas las copias y las consultas por `railway api`—, pero conviene resolverlo cuando toque, aunque solo sea para dejar de arrastrarlo.
- **Alta como reutilizador en el portal municipal** (SPEC.md §2.2): sigue pendiente. No bloquea nada (toda la API es GET público sin clave), pero conviene hacerlo antes de desplegar, registrar las URL consumidas (catálogo, Swagger, federación, juntas y el detalle de cada junta, y una distribución por ficha al día; en fase 2 se añadirán las quejas) y aprovechar para pedir inversión por junta y presupuestos participativos como datos abiertos. datos.gob.es no requiere alta.
- **Avisar al ayuntamiento** (Gobierno Abierto, `gobiernoabierto@zaragoza.es`, o su delegado de protección de datos) de que el texto libre de quejas y sugerencias sale por la API sin anonimizar (S0.3 adenda). Y, en el mismo aviso o en `datosabiertos@zaragoza.es` (el contacto que publica el Swagger), de lo que el monitor detecta en el catálogo: 8 servicios API inexistentes (S1.1), 23 distribuciones WFS/WMS de intranet (`-lan`), `puntos-interes` que responde vacío a `sort`, una URL con `https:/`, 8 tags declarados que el Swagger no documenta, la tilde de `clavo-topográfico`, los 4 índices con `jsessionid` declarados como endpoint (S1.2), las 15 fichas abiertas sin federar en datos.gob.es y el hecho de que `catalogo.json` no devuelve las partes de series (S1.3). **Y lo que ha salido en S2.1**: `point`/`distance` está documentado en el Swagger de `portalero/v2` y de `registro-licencia/portal` y **no filtra por proximidad** (este último responde 400 filtrando un `SQLGrammarException` de Hibernate al exterior, que además es una fuga de detalle interno); el filtro FIQL `q=junta.id==N` de `portalero/v2` no filtra por junta; `junta.title` llega sin `id` y `CASCO HISTÓRICO` con un carácter no imprimible (U+0093); el callejero y las quejas usan `DISTRITO SUR` y `SAN JUAN DE MOZARRIFAR`, que no son los nombres oficiales de las juntas; y los polígonos de `distrito.json` se solapan en el entorno de Juslibol (con Alfocea, Monzalbarba y Actur). **Y lo de S2.2, que es lo más serio de todo**: el texto libre de las quejas sigue saliendo sin anonimizar y el 47,9 % de una muestra de 7.000 registros contiene una fórmula de firma, es decir, nombres propios escritos por la persona que reclama; el listado de sede publica 89.432 registros frente a las ~40.000 incidencias cerradas al año que cuenta su propio `statistics.json`, sin que se sepa el criterio; el orden por defecto de `quejas-sugerencias/list.json` no está documentado y cambió entre el 5 y el 8 de septiembre, lo que rompe en silencio a cualquiera que pagine sin `sort`; y `district` llega con `DISTRITO SUR`, `SAN JUAN DE MOZARRIFAR`, `TORRECILLA` y un `CASCO HISTÓRICO` con un carácter de control U+0093 dentro. **Y lo que salió al ingerir los 89.432 de verdad**: paginar el listado por `updated_datetime` pierde 8.804 registros por empates de fecha (quien lo haga sin saberlo se lleva un subconjunto creyendo que tiene el todo); `quejas-sugerencias/412792.json` responde 400 con `"Index: 0, Size: 0"` —un error interno filtrado al exterior— mientras el listado sirve ese mismo registro sin problema; y `status=rejected` como filtro se ignora en silencio y devuelve cerradas, que es el mismo patrón del `q=junta.id==N` de S2.1: parámetros que se aceptan y no se aplican.
- Remoto: `origin` = <https://github.com/MarioNaya/zaragoza-observatorio>; `main` integra la fase 1 completa, el despliegue, las copias y —desde el 2026-09-08— S2.1, `geo`, S2.2 y `citizen`. Pendiente solo decidir si se borran las ramas `feat/*` ya integradas (§3 punto 7).
- Si se quiere usar el 8080, parar el contenedor phpMyAdmin que lo ocupa.

## 7. Dudas abiertas

Listadas en `SPEC.md` §9. Las que tocan al cierre de la fase 1: categoría observada y cruce declarado/observado (ADR tras la primera serie), umbrales de frescura, retención de `raw_payload` (14 días provisional), qué hacer con las fichas sin distribución observable (151) más allá de declararlo en `caveats`, la ingesta de las partes de series y colecciones que el listado omite (108 federadas; fase 2 con spike), la **imagen nativa con GraalVM** (bajaría de ~356 MB a 80–150 MB; aplazada en ADR-009), la **limpieza del clúster de Postgres 18 que quedó en el volumen** y, de las copias, lo que no se ha probado: **una restauración de verdad** y el hecho de que todas vivan en el mismo proveedor (ADR-010). Nueva del 2026-09-08: **las fichas que desaparecen del catálogo** no se ven en la API (436 → 434; `lastSeenAt` lo sabe, nadie lo publica; §4 punto 6). De la fase 2 (S2.1, ADR-011): el **solape de los polígonos** en el entorno de Juslibol —se registra la ambigüedad, no se recorta el dato oficial— y las **variantes de nombre** de junta, que quedan **resueltas**: la tabla de sinónimos que ADR-011 §5 pedía existe en `geo.DistrictNames` desde el 2026-09-08, con las **cuatro** variantes que S2.2 observó (`DISTRITO SUR`, `SAN JUAN DE MOZARRIFAR`, `TORRECILLA` y el `CASCO HISTÓRICO` con U+0093), no las dos previstas. La correspondencia `distrito.id` ↔ `idpadron` también queda **resuelta**. De la fase 2 (S2.2, ADR-012): el **texto libre queda resuelto** —no se pide—, y se abre en su lugar la **comparabilidad entre años** de las series de quejas por junta, porque la cobertura de punto va del 16 % al 45 % según el año; hoy cada grupo publica su cobertura al lado y lo que falta es medirla sobre la carga completa y decidir si cabe ofrecer algo más (§4). Siguen abiertos el **criterio de publicación** del listado de sede (89.432 registros frente a ~40.000 cerradas/año) y qué hacer con los servicios **`INTERNAL`**. La exposición de `/actuator/modulith` en producción queda **resuelta**: no se expone (ADR-008 §6). La copia del volumen queda **resuelta** (ADR-010), pero no por la vía prevista: la IaC de Railway no aplica ese campo.

## 8. Mapa del repositorio

```
SPEC.md                         especificación viva (v0.13, ADR-000)
CLAUDE.md                       reglas de trabajo (1–29) y contexto operativo
README.md                       presentación breve, enlaces y API
docs/ESTADO.md                  este documento
docs/despliegue.md              runbook de despliegue en Railway (IaC, variables, comprobaciones)
.railway/railway.ts             infraestructura en código: base de datos PostGIS y servicio de la app (ADR-008)
package.json, package-lock.json  única dependencia: el SDK `railway` que importa .railway/railway.ts
docs/arquitectura.md            diagramas Mermaid (flujo de datos, módulos, hexagonal con clases reales)
docs/arquitectura.html          página HTML de la fase 0, CONGELADA (nombres previos a la implementación, `geo` como pendiente): manda el .md
docs/decisions/                 ADR-000..012
docs/spikes/                    informes S0.1..S0.6, S1.1..S1.3, S2.1 y S2.2, matriz CSV, índice
pom.xml, compose.yaml           Boot 4.1.1, Modulith 2.1.1, Resilience4j 2.4.0, springdoc 3.1.0, perfil -Pspikes
Dockerfile, .dockerignore       imagen de producción multietapa (JDK 21 -> JRE 21, no root, capas de Boot); ADR-008
compose.prod.yaml               prueba local de esa imagen con el perfil prod (proyecto y base de datos propios)
src/main/resources/application.yaml            spring.http.clients.*, zaragoza.http.user-agent, zaragoza.ingestion.*, zaragoza.catalog.* (freshness, observation, api-inventory, federation)
src/main/resources/db/migration/               V001 postgis · V002 event_publication · V003 ingestion · V004 catalog · V005 eje observado · V006 inventario Swagger · V007 federación · V008 geo (juntas con geometría y padrón) · V009 citizen (quejas, sin columna de texto libre)
src/main/java/es/zaragoza/observatory/
  ClockConfiguration, HttpClientConfiguration, OpenApiConfiguration   configuración de aplicación (Clock y RestClient únicos)
  shared/                                      DatasetRef, Sources (data-space, sede, ocds, open311, datos-gob-es), IngestionRunId, DatasetIngested, ZaragozaTime, HttpDates
  ingestion/                                   API (SourceDescriptor con Pagination NONE/OFFSET/PAGE y ResponseShape ENVELOPE/ARRAY/DOCUMENT/RESULT_ITEMS, RawPage, IngestionJob, Ingestion…), domain, application, infrastructure
  geo/                                         API (Geo, GeoSources, GeoPoint, DistrictLocation, DistrictNames con los sinónimos, DistrictSummary con el denominador);
                                               domain (District, DistrictKind, Boundary, PopulationRecord, DistrictRepository, DistrictLocator, PopulationRepository, DistrictProfileReader);
                                               application (GeoService, RegisterDistricts, UpdateDistrictProfiles);
                                               infrastructure (zaragoza: DistrictJsonTranslator, DistrictsIngestionJob, DistrictProfileHttpReader; persistence: JpaDistrictRepository con ST_GeomFromGeoJSON, PostgisDistrictLocator con ST_Contains, GeoJson; events; web: GeoController, GeoDtos, GeoCaveats)
  citizen/                                     API (CitizenSources: REQUESTS, CLOSURES); domain (ServiceRequest sin texto, ServiceRequestStatus, Assignment, DistrictAssignment, ServiceRequestDraft, consultas y agregaciones, ServiceRequestRepository);
                                               application (RegisterServiceRequests: resuelve el territorio del lote entero y persiste);
                                               infrastructure (zaragoza: ServiceRequestJsonTranslator, ServiceRequestsIngestionJob + NewRequests/ClosedRequests con marca de agua; persistence: JdbcServiceRequestRepository; web: CitizenController, CitizenDtos, CitizenCaveats)
  catalog/                                     API (CatalogSources: CATALOG, API_INVENTORY, FEDERATION); domain (Dataset, FreshnessSnapshot, Observation, ObservationMethod, ApiEndpoint, FederatedDataset, puertos y read models);
                                               application (RegisterDatasets, TakeFreshnessSnapshots, ObserveDatasets, RecordObservation, RegisterApiEndpoints, RegisterFederatedDatasets);
                                               infrastructure (zaragoza: traductores, jobs, DistributionHttpObserver, ObservationUrls; persistence; events; scheduling; web: CatalogController, ApiInventoryController, FederationController, Sorting, Caveats)
src/test/java/es/zaragoza/observatory/
  ModularityTests, HexagonalArchitectureTests, *IntegrationTests, CatalogDataQualityTest, support/Fixtures (bytes, text, headers)
  catalog/support/                             InMemoryDatasets, InMemorySnapshots, InMemoryApiEndpoints (dobles de los puertos)
  citizen/support/                             InMemoryServiceRequests, FakeGeo (dobles de los puertos y de la API de geo)
  spikes/                                      S01..S06, S11, S12, S13, S21 + support/ (SpikeFixtures.saveRedacted/saveHeaders, ZaragozaSpikeClient.head/tryGet)
src/test/resources/fixtures/zaragoza/          catalog (rows2-fl, rows500-fl, 404 con cabeceras; swagger-api.json + .headers; datos-gob-es page0/last/beyond; observation/: HEAD, rows=1, sort, WFS hits, /list), ocds, open311, geo, inventory
```
