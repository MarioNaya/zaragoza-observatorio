# S0.3 — Quejas y sugerencias: Open311 y endpoint de sede

- Fecha de ejecución: 2026-09-05
- Clase: `S03Open311Spike` (`.\mvnw.cmd test -Pspikes "-Dtest=S03*"`)
- Fixtures: `src/test/resources/fixtures/zaragoza/open311/` (`services.json`, `requests-default.json`, `requests-closed-2026-07.json`, `request-947147.json`, `sede-list-rows50.json`, `sede-list-500-recent.json`, `sede-detail-316864.json`, `sede-statistics-2025.json`)
- Métricas completas: `target/spikes/S0.3-open311.md`

## Pregunta

Cuál es el endpoint real de quejas y sugerencias, qué campos trae, si las incidencias vienen geolocalizadas y asignadas a territorio, si hay fecha de cierre para calcular tiempo de respuesta, cómo es la taxonomía, y qué volumen y profundidad histórica hay (SPEC.md §3 S0.3, módulo `citizen`).

## Método

Dos familias de endpoints, ambas GET públicas sin firma HMAC (la firma solo aplica al POST de alta de incidencias):

- Open311: `https://www.zaragoza.es/api/recurso/open311/services.json`, `requests.json`, `requests/{id}.json`, `services/{code}.json`.
- Sede: `https://www.zaragoza.es/sede/servicio/quejas-sugerencias/list.json`, `quejas-sugerencias/{id}.json`, `quejas-sugerencias.json`, `quejas-sugerencias/statistics.json?year=`, `list.geojson`.

Se probaron paginación (`rows`, `start`, `page`, `page_size`), filtros (`status`, `service_code`, `start_date`/`end_date`, `barrio_code`, `q` FIQL), orden (`sort`), sistema de referencia (`srsname=wgs84`) y ventanas anuales/semanales para medir profundidad y topes.

## Resultados

### Taxonomía (`services.json`)

100 servicios (`service_code`, `service_name`, `description`, `metadata=false`, `type=realtime`); solo 23 con descripción. Los códigos son heterogéneos (`250`, `18014`, `1000007`, `97550336`, `188088321`). `services/{code}.json` devuelve los atributos del formulario de alta (`account_id` obligatorio para POST). `statistics.json?year=YYYY` devuelve por categoría `{id, title, closed, parent}`: todas las categorías traen `parent`, es decir, hay una jerarquía que no está en `services.json`.

### Open311 `requests.json`

| Aspecto | Observado |
|---|---|
| Página por defecto | 50 registros; `rows=500` devuelve 500; `start` desplaza (`start=50` cambia el primero); `page` y `page_size` se ignoran |
| Campos siempre presentes | `service_request_id`, `status` (`open`/`closed`), `service_code`, `service_name`, `title`, `description`, `requested_datetime`, `updated_datetime` |
| Campos parciales (50 recientes) | `lat`/`long` 22 (44 %), `address_string` 17, `address_id` 14, `district` 14 (28 %) |
| Filtros | `status`, `service_code`, `start_date`/`end_date` en formato `2026-08-01T00:00:00Z` funcionan; el tope por petición sigue siendo `rows` (el mes de agosto devuelve 50 sin `rows`, y 50 por cada semana: hay que paginar) |
| Histórico | Hay datos desde **2017**; ventanas anuales de 2004–2016 devuelven 0 |
| Detalle | `requests/{id}.json` devuelve los mismos campos que el listado |
| Cabeceras | `ETag` presente pero `If-None-Match` devuelve 200 (no se honra, ver S0.5) |

Cerradas de julio 2026 (muestra de 50): `lat/long` 16, `district` 11; horas entre `requested_datetime` y `updated_datetime`: p10 = 14 h, p50 = 26 h, p90 = 454 h, máx 796 h.

### Sede `quejas-sugerencias/list.json`

| Aspecto | Observado |
|---|---|
| Forma | array JSON (no `{totalCount,result}`); `.geojson` devuelve `FeatureCollection` en CRS84 |
| Orden por defecto | ascendente por fecha: el primer registro es de **2013-01-08** → histórico desde 2013 (Open311 solo desde 2017) |
| Paginación | `rows` tope **500** (`rows=1000` devuelve 500); `start` funciona hasta al menos `start=50000` (primer registro 2023-01-04); `start=100000` devuelve 200 con cuerpo vacío. Con orden ascendente por fecha, 50.000 registros cubren 2013–2022: **el listado público tiene entre 50.000 y 100.000 registros en total**, muy por debajo de las ~40.000 cerradas/año de `statistics` → es un subconjunto (hipótesis: solo las validadas/publicadas; el Swagger tiene un parámetro `validated`) |
| Orden | `sort=requested_datetime asc|desc` y `sort=updated_datetime desc` funcionan → base para ingesta incremental |
| Filtros | `status=closed` funciona; `service_code=250` funciona; FIQL: `q=service_request_id==947147`, `q=service_request_id=ge=500000;service_request_id=lt=500100` y **`q=requested_datetime=ge=2020-01-01T00:00:00Z`** funcionan; `q=status==closed` devuelve 400 "search condition is empty"; **`barrio_code` devuelve 400** (`Property path 'portal.junta.id' invalid`: parámetro roto aunque está en el Swagger); `start_date`/`end_date` devuelven 400 con todos los formatos probados (usar FIQL en su lugar) |
| Campos (500 más recientes, 25 ago–4 sep 2026) | siempre: `service_request_id`, `status`, `service_code`, `service_name`, `requested_datetime`; `geometry` (Point) 251 (50 %); `address_string` 229; `district` 200 (40 %); `updated_datetime` 216; **`barrio_code` 0**, `catSip` 0 |
| `updated_datetime` | 216 rellenos = exactamente los 216 `closed` de la muestra: **se informa al cerrar** → sirve como fecha de cierre |
| `district` | nombre de junta en mayúsculas (24 valores en la muestra, con duplicado por codificación de la tilde en `CASCO HISTÓRICO`), no un código |
| Ids | compartidos con Open311 para lo reciente (947147 existe en ambos); los antiguos (316864, 2013) solo existen en sede |
| Detalle | `quejas-sugerencias/{id}.json` mismos campos + `geometry` |
| Cabeceras | sin `ETag` ni `Last-Modified` |

Volumen anual de **cerradas** según `statistics.json` (suma de `closed` por categoría): 2023 → 44.658, 2024 → 36.691, 2025 → 42.017, 2026 (hasta el 5 de septiembre) → 33.822. Orden de magnitud: **unas 40.000 incidencias al año**, ~500.000 desde 2013 (los ids van de ~317.000 en 2013 a 947.000 en 2026, pero no son densos).

Categorías más frecuentes en las 500 recientes: Transporte Público (29), Señalización (27), Acera (26), Césped/arbustos (23), Alumbrado (19), Instalaciones Deportivas (17), Basura abandonada (15), Basura alrededor de contenedores (15), `INTERNAL` (14), Policía Local (13).

## Recomendación

1. **Fuente de ingesta de `citizen`: el endpoint de sede** `quejas-sugerencias/list.json` con `rows=500`, `srsname=wgs84` y `fl` explícito (`service_request_id,status,service_code,service_name,title,description,requested_datetime,updated_datetime,geometry,address_string,district`). Motivos: histórico desde 2013, `geometry` en lugar de `lat/long` sueltos, `district` y `sort`. Open311 queda para contrastar y para `services.json` (taxonomía).
2. **Carga inicial** por ventanas FIQL de fecha (`q=requested_datetime=ge=A;requested_datetime=lt=B`, por meses) con `rows=500` + `start`: evita depender de offsets profundos y es reanudable. Entre 50.000 y 100.000 registros en total: la carga completa son cientos de peticiones, no miles.
3. **Ingesta incremental** con FIQL sobre `requested_datetime` (altas) y, si se confirma que el campo es filtrable, `updated_datetime` (cierres) desde la última marca conocida; como respaldo, `sort=updated_datetime desc` paginado hasta alcanzarla. No hay `If-Modified-Since`. Upsert idempotente por `service_request_id`.
4. **Modelo `ServiceRequest`** (ajuste de §4.6): `sourceId`, `status`, `serviceCode`, `serviceName`, `title`, `description`, `requestedAt`, `closedAt` (= `updated_datetime` solo si `status=closed`; nulo en abiertas), `point` (nullable, WGS84), `addressText` (nullable), `districtName` (nullable, tal cual), `neighbourhoodId` (resuelto por PostGIS desde `point`, nullable). `Category` = taxonomía de `services.json` más la jerarquía `parent` de `statistics`. Sin `channel`: no existe en los datos.
5. **Tiempo de respuesta**: calculable solo en cerradas (`closedAt - requestedAt`); exponer siempre el número de abiertas excluidas. La muestra de julio (p50 26 h, p90 454 h) es pequeña y sesgada: no publicar cifras hasta tener la carga completa.
6. **Territorio y sesgo** (§1.1): solo ~50 % de las incidencias recientes tienen punto y ~40 % junta; el resto no se puede asignar. Toda agregación territorial debe informar `unassigned` además del denominador de población, y la advertencia de que la geolocalización depende del canal de entrada. `barrio_code` no existe en la práctica: la asignación a barrio será por PostGIS (S0.4).
7. **Calidad**: normalizar `district` (tildes con doble codificación) solo como texto de origen; la referencia territorial real sale del punto.

## Impacto en SPEC.md

- §2 tabla: fila Open311 → "Confirmado": `https://www.zaragoza.es/api/recurso/open311/requests.json` (GET público) y fuente principal `sede/servicio/quejas-sugerencias/list.json` (histórico 2013, geometría, junta).
- §3 fase 2: la asignación a barrio es solo para el ~50 % geolocalizado; tiempo de respuesta solo en cerradas.
- §4.6 `citizen`: modelo según recomendación 4; eliminar `canal`.
- §4.7: `GET /citizen/aggregations` debe devolver `unassigned` junto al denominador.
- §5: ~40.000 incidencias/año, ~500.000 históricas: la carga inicial es de decenas de minutos, no de horas.

## Riesgos y dudas abiertas

- El listado de sede (50.000–100.000 registros) es un subconjunto de las ~40.000 incidencias/año que cuenta `statistics`: hay que entender qué se publica y qué no (¿`validated`?) antes de hablar de volúmenes por territorio. Documentarlo como caveat.
- `start_date`/`end_date` en sede devuelven 400 con todos los formatos probados; en Open311 funcionan con `...T00:00:00Z`. En sede el filtro temporal es FIQL sobre `requested_datetime`; confirmar en fase 1 que `updated_datetime` también admite FIQL.
- Duplicidad de nombres de junta por codificación (`CASCO HISTÓRICO` ×2): tratar como texto, no como clave.
- Las incidencias con `status=open` de julio 2026 siguen abiertas (50 en la muestra): el tiempo de respuesta tendrá censura por la derecha; documentarlo en `caveats`.
- Los servicios `INTERNAL` (14/500) no son quejas ciudadanas: decidir en fase 2 si se excluyen o se etiquetan (decisión documentada, regla 6).

## Adenda 2026-09-06: fixtures redactados

Al revisar el repositorio antes de publicarlo se comprobó que el ayuntamiento devuelve el texto libre de las quejas (`title`, `description`, y `service_notice` en el listado de sede) **sin anonimizar**: en `requests-closed-2026-07.json` aparecía el nombre completo de una ciudadana y su DNI dentro de la descripción, y en varios fixtures había firmas y despedidas. Se hizo lo siguiente:

- Los fixtures `request-947147.json`, `requests-closed-2026-07.json`, `requests-default.json`, `sede-detail-316864.json` y `sede-list-rows50.json` se sustituyeron por versiones con esos tres campos reemplazados por el marcador `[texto omitido en el fixture: puede contener datos personales]`. El resto de campos (id, fechas, estado, servicio, `address_string`, `district`, geometría) se conserva, que es lo que usan las métricas de este informe. `services.json`, `sede-statistics-2025.json` y `sede-list-500-recent.json` no tienen texto ciudadano y no cambian.
- Los originales se eliminaron de todo el historial con `git filter-repo` y las ramas se volvieron a subir.
- `S03Open311Spike` guarda ahora esos fixtures con `SpikeFixtures.saveRedacted(..., CITIZEN_TEXT)`, de modo que reejecutar el spike no vuelve a introducir el texto (CLAUDE.md regla 22).

Consecuencia para la fase 2: el módulo `citizen` no debe republicar `title`/`description` tal cual (SPEC.md §4.6 y §9). Conviene comunicar el hallazgo al ayuntamiento (Gobierno Abierto o su delegado de protección de datos) al darse de alta como reutilizador.
