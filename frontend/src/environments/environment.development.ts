/**
 * En desarrollo la aplicación corre en el 8085, no en el 8080, que en esta máquina lo ocupa otro proyecto
 * (CLAUDE.md, contexto operativo).
 */
export const environment = {
  apiBaseUrl: 'http://localhost:8085',
};
