/**
 * Kernel compartido mínimo (SPEC.md §4.3): tipos de valor comunes y eventos base.
 * No depende de ningún otro módulo.
 */
@ApplicationModule(displayName = "Shared kernel", allowedDependencies = {})
package es.zaragoza.observatory.shared;

import org.springframework.modulith.ApplicationModule;
